import type { Engine } from '@ineka/engine'
import { OuterNode } from '@ineka/engine'

export class ProgressionSystem extends OuterNode {
  constructor(parent: Engine) {
    super(parent)
  }
}
